<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ptu.noriteo.mapper.KakaoUsersMapper">

    <!-- 카카오 유저 찾기 -->
    <select id="findByProviderId" parameterType="String" resultType="com.ptu.noriteo.model.Users">
        SELECT * FROM USERS
        WHERE PROVIDER = 'kakao'
          AND PROVIDER_ID = #{providerId}
    </select>

    <!-- 카카오 유저 등록 -->
    <insert id="insertKakaoUser" parameterType="com.ptu.noriteo.model.Users">
        INSERT INTO USERS (USER_ID, PROVIDER, PROVIDER_ID, USER_EMAIL, USER_NAME, ROLE_ID)
        VALUES (USERS_SEQ.NEXTVAL, #{provider}, #{providerId}, #{userEmail}, #{userName}, #{roleId})
    </insert>


</mapper>
