<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ptu.noriteo.mapper.NaverUsersMapper">

    <!-- 네이버 유저 조회 -->
    <select id="findByProviderId" parameterType="String" resultType="com.ptu.noriteo.model.Users">
        SELECT * FROM USERS
        WHERE PROVIDER = 'naver'
          AND PROVIDER_ID = #{providerId}
    </select>


    <!-- 네이버 유저 등록 -->
    <insert id="insertNaverUser" parameterType="com.ptu.noriteo.model.Users">
        INSERT INTO USERS (
            USER_ID, PROVIDER, PROVIDER_ID, USER_EMAIL,
            USER_NAME, GENDER, MOBILE, PHONE,
            BIRTH, ROLE_ID, ORIGIN_USER, SYS_USER
        )
        VALUES (
                   USERS_SEQ.NEXTVAL, #{provider}, #{providerId}, #{userEmail},
                   #{userName}, #{gender, jdbcType=VARCHAR}, #{mobile, jdbcType=VARCHAR}, #{phone, jdbcType=VARCHAR},
                   #{birth, jdbcType=VARCHAR}, #{roleId}, #{originUser, jdbcType=VARCHAR}, #{sysUser, jdbcType=VARCHAR}
               )
    </insert>


</mapper>

